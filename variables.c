#include"external_variable.h"
#include<stdio.h>
typedef unsigned long long int ull;
int x;// default value of global variable is 0
int main()
{
	int y,z;
	ull n = 1000000;
	printf("Value of external variable is: %d",num);
	printf("\nx = %d, y = %d, z = %d",x,y,z);
	printf("\nThe value of unsigned long long int is: %d", n);
}
